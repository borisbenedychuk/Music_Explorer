<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.geniussearch">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/art_placeholder"
        android:label="@string/app_name"
        android:roundIcon="@drawable/art_placeholder"
        android:supportsRtl="true"
        android:theme="@style/Theme.GeniusSearch">
        <activity android:name=".ArtistActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".SongActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>